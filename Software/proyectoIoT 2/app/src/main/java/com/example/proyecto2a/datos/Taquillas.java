package com.example.proyecto2a.datos;

import com.example.proyecto2a.modelo.Stant;
import com.example.proyecto2a.modelo.Taquilla;
import com.google.firebase.firestore.CollectionReference;
import com.google.firebase.firestore.DocumentReference;
import com.google.firebase.firestore.FirebaseFirestore;

public class Taquillas {
    /*
    private static DocumentReference taquillas;
    Taquilla taquilla = new Taquilla();

    public Taquillas(){
        Stant stant = new Stant();
        FirebaseFirestore db = FirebaseFirestore.getInstance();
        //taquillas = db.collection("estaciones").document(stant.getuID()).collection("taquillas").document(taquilla.getId());
    }

    //Crear una taquilla nueva
    public void actualizarTaquilla(Taquilla taquilla) {
        taquillas.collection(taquilla.getId()).add(taquilla);
    }

     */
}
